import { create } from '@storybook/theming/create';

export default create({
  base: 'light',

  brandTitle: 'react-alternating-timeline',
  brandUrl: 'https://github.com/openscript-ch/react-alternating-timeline',
});
